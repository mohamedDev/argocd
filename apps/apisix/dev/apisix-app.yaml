apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: apisix-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/mohamedDev/argocd.git'
    targetRevision: HEAD
    path: charts/apisix
    helm:
      valueFiles:
        - ../../apps/apisix/dev/values.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: apisix-dev    # 👈 le namespace cible
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true     # 👈 ArgoCD créera le namespace si absent
